# Copy this file to `.env` and fill in real values.
# Never commit `.env` (it is gitignored).
#
# NOTE: These are for LOCAL testing only (test-workflow-local.js).
# For n8n cloud, configure credentials in the n8n UI.
# For GitHub Actions, secrets are stored as GH_PAT and OPENAI_API_KEY
# (GitHub reserves the GITHUB_ prefix for its own use).

# GitHub Personal Access Token (fine-grained or classic)
# Needs read access to this repo's contents via GitHub API.
GITHUB_TOKEN=

# OpenAI API Key
OPENAI_API_KEY=

# Optional (future)
# CANVA_API_KEY=
# LINKEDIN_CLIENT_ID=
# LINKEDIN_CLIENT_SECRET=
# LINKEDIN_REDIRECT_URI=

# ============================================
# WHATSAPP APPROVAL (Twilio)
# ============================================
# Get credentials at: https://console.twilio.com
# WhatsApp sandbox setup: Console → Messaging → Try it out → Send a WhatsApp message

TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886
WHATSAPP_TO=whatsapp:+1YOURNUMBER

# Webhook for receiving replies (use ngrok for local dev)
WEBHOOK_PORT=3000
WEBHOOK_PATH=/whatsapp/incoming

# ============================================
# HYBRID IMAGE GENERATOR (DALL-E 3)
# ============================================
# Enable hybrid image generator in workflows (default: false for backwards compatibility)
# When true: Uses DALL-E backgrounds + Puppeteer compositor
# When false: Uses legacy image generator (solid colors + SVG)
HYBRID_IMAGES_ENABLED=false

# Enable/disable DALL-E API calls (set to 'false' to use cache only during testing)
DALLE_ENABLED=true

# DALL-E 3 Configuration (uses same OPENAI_API_KEY above)
DALLE_MODEL=dall-e-3
DALLE_QUALITY=standard
DALLE_SIZE=1024x1024

# Set to 'false' to reduce console output
DALLE_VERBOSE=true
