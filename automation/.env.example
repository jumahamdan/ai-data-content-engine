# Copy this file to `.env` and fill in real values.
# Never commit `.env` (it is gitignored).
#
# NOTE: These are for LOCAL testing only.
# For GitHub Actions, store these as repository secrets.

# ============================================
# CONTENT GENERATION (Claude API)
# ============================================
# Get from: https://console.anthropic.com/
ANTHROPIC_API_KEY=

# OpenAI API Key (used for DALL-E image generation)
OPENAI_API_KEY=

# ============================================
# LINKEDIN POSTING (OAuth 2.0)
# ============================================
# App setup: https://developer.linkedin.com/
# Required product: "Share on LinkedIn"
# Run `node linkedin-auth/get-token.js` to get initial tokens.
LINKEDIN_CLIENT_ID=
LINKEDIN_CLIENT_SECRET=
LINKEDIN_ACCESS_TOKEN=

# ============================================
# WHATSAPP APPROVAL (Twilio)
# ============================================
# Get credentials at: https://console.twilio.com
# WhatsApp sandbox setup: Console → Messaging → Try it out → Send a WhatsApp message

TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886
WHATSAPP_TO=whatsapp:+1YOURNUMBER

# Webhook for receiving replies (use ngrok for local dev)
WEBHOOK_PORT=3000
WEBHOOK_PATH=/whatsapp/incoming

# ============================================
# HYBRID IMAGE GENERATOR (DALL-E 3)
# ============================================
# Enable hybrid image generator in workflows (default: false for backwards compatibility)
# When true: Uses DALL-E backgrounds + Puppeteer compositor
# When false: Uses legacy image generator (solid colors + SVG)
# Provider selection: see IMAGE_PROVIDER below
HYBRID_IMAGES_ENABLED=false

# Image provider selection (replaces deprecated DALLE_ENABLED / GEMINI_ENABLED)
# Valid: 'gemini', 'dalle', 'auto', 'none'
# - gemini: Use Gemini exclusively (~$0.039/image)
# - dalle: Use DALL-E exclusively (~$0.08/image)
# - auto: Prefer Gemini if GEMINI_API_KEY set, fallback to DALL-E (default)
# - none: Skip AI generation, use CSS fallback colors
IMAGE_PROVIDER=auto

# DALL-E 3 Configuration (uses same OPENAI_API_KEY above)
DALLE_MODEL=dall-e-3
DALLE_QUALITY=standard
DALLE_SIZE=1024x1024

# Set to 'false' to reduce console output
DALLE_VERBOSE=true

# ============================================
# GEMINI IMAGE GENERATOR (Google Gemini API)
# ============================================
# Google AI API Key (get from: https://aistudio.google.com/apikey)
# Required when IMAGE_PROVIDER=gemini or IMAGE_PROVIDER=auto
GEMINI_API_KEY=

# Gemini model (default: gemini-2.5-flash-image at ~$0.039/image)
# Alternative: gemini-3-pro-image-preview (~$0.134/image, higher quality)
GEMINI_MODEL=gemini-2.5-flash-image

# Image generation parameters
GEMINI_ASPECT_RATIO=1:1
GEMINI_IMAGE_SIZE=1K

# Set to 'false' to reduce console output
GEMINI_VERBOSE=true
